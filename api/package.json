{
  "name": "@decklist/api",
  "version": "0.0.1",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc src/index.ts --declaration --outDir dist"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}